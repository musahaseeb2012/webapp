# Jarvis

Generated by `build-pages.sh` on the `claude/jarvis-ai-chatbot-320s6p` branch.
Nothing in this folder is edited by hand — rebuild there and copy `site/` here.

Served over https so the microphone works; a `file://` page cannot ask for it.
