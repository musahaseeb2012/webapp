# Jarvis

Generated by `build-pages.sh` on the `claude/jarvis-ai-chatbot-320s6p` branch —
nothing here is edited by hand. Rebuild there and copy `site/` into this folder.

Served over https so the microphone works: a page opened from disk (`file://`)
cannot ask for microphone permission at all.
